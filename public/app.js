const form=document.querySelector('#form');const input=document.querySelector('#input');const chat=document.querySelector('#chat');const clear=document.querySelector('#clear');let messages=[];function add(role,text){const el=document.createElement('div');el.className=`message ${role}`;el.textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight}form.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;input.value='';add('user',text);messages.push({role:'user',content:text});const button=form.querySelector('button');button.disabled=true;add('assistant','Thinking…');const pending=chat.lastChild;try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages})});const data=await r.json();pending.textContent=data.reply||data.error||'No response.';if(data.reply)messages.push({role:'assistant',content:data.reply})}catch{pending.textContent='Connection error.'}finally{button.disabled=false;input.focus()}});clear.addEventListener('click',()=>{messages=[];chat.innerHTML='<div class="welcome">Ask darkAI anything.</div>';input.focus()});